<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- For PermissionDemo file -->
    <!--  -->
    <!-- <uses-feature -->
    <!-- android:name="android.hardware.camera" -->
    <!-- android:required="false" /> -->
    <!-- <uses-permission android:name="android.permission.CAMERA" /> -->
    <!-- <uses-feature -->
    <!-- android:name="android.hardware.bluetooth" -->
    <!-- android:required="false" /> -->
    <!-- <uses-permission android:name="android.permission.BLUETOOTH" /> -->
    <!--  -->
    <!-- <uses-permission android:name="android.permission.BLUETOOTH" /> -->
    <!-- <uses-permission -->
    <!-- android:name="android.permission.CAMERA" -->
    <!-- tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" /> -->
    <!-- <uses-permission android:name="android.permission.RECORD_AUDIO" /> -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.MANAGE_DEVICE_POLICY_MICROPHONE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndriodStudio"
        tools:targetApi="31">
        <activity
            android:name=".Intent_login_2"
            android:exported="false" />
        <activity
            android:name=".Intent_login_1"
            android:exported="false" />
        <activity
            android:name=".Intent_class"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Intent_class_2"
            android:exported="true" />
    </application>

</manifest>